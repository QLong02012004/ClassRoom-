# BỘ KỊCH BẢN KIỂM THỬ CHI TIẾT TOÀN DIỆN DÀNH CHO GIÁO VIÊN (TEACHER DETAILED TEST CASES)

> **Mục đích:** Danh sách kịch bản kiểm thử (Test Cases) chi tiết với đầy đủ các trường hợp kiểm thử (Positive, Negative, Boundary, Security, Edge Cases) cho từng tính năng dành riêng cho Giáo viên (Teacher) trên hệ thống Quản lý Học tập ClassRoom.

---

## 📌 THÔNG TIN TÀI KHOẢN GIÁO VIÊN MẶC ĐỊNH
- **Đường dẫn đăng nhập:** `http://localhost:5173/login`
- **Email Giáo viên thử nghiệm:** `teacher@gmail.com`
- **Mật khẩu Giáo viên:** `teacher123` hoặc `123456`
- **Email Học sinh thử nghiệm:** `student@gmail.com` / `123456`
- **Email Admin thử nghiệm:** `admin@gmail.com` / `admin123`

---

## 🛠️ CHI TIẾT CÁC KỊCH BẢN KIỂM THỬ (TEST CASES & SUB-CASES)

---

### MODULE 1: XÁC THỰC, ĐĂNG KÝ & QUẢN LÝ HỒ SƠ (`/login`, `/register`, `/profile`)

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-01** | **Đăng nhập Giáo viên thành công** | **01.1 (Positive)**: Đăng nhập hợp lệ bằng tài khoản Giáo viên | 1. Mở `/login`<br>2. Nhập Email & Mật khẩu Giáo viên<br>3. Bấm "Đăng nhập" | Email: `teacher@gmail.com`<br>Pass: `teacher123` | - Đăng nhập thành công, xuất hiện Toast chào mừng.<br>- Tự động chuyển hướng vào `/classrooms`. | `[ ] Pass`<br>`[ ] Fail` |
| | | **01.2 (Negative)**: Nhập sai Mật khẩu | 1. Nhập Email chuẩn<br>2. Nhập sai Mật khẩu<br>3. Bấm "Đăng nhập" | Email: `teacher@gmail.com`<br>Pass: `wrongpass` | Hiển thị Toast thông báo lỗi: *"Tài khoản hoặc mật khẩu không chính xác!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **01.3 (Negative)**: Để trống Email hoặc Mật khẩu | 1. Để trống thông tin<br>2. Bấm "Đăng nhập" | Form trống | Hiển thị thông báo validation ngay bên dưới ô nhập: *"Vui lòng nhập Email / Mật khẩu"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **01.4 (Security)**: SQL Injection / Script XSS trong ô Đăng nhập | 1. Nhập chuỗi SQL/XSS vào ô Email & Mật khẩu | `' OR '1'='1`<br>`<script>alert(1)</script>` | Hệ thống lọc dữ liệu an toàn, báo lỗi đăng nhập không hợp lệ, không bị dính XSS hay SQL Injection. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-01B** | **Đăng nhập & Đăng ký bằng Google OAuth 2.0** | **01B.1 (Positive)**: Đăng nhập/Đăng ký 1-Click qua Google | 1. Mở `/login` hoặc `/register`<br>2. Click nút **[ Đăng nhập bằng Google ]**<br>3. Chọn tài khoản Google | Google Credentials | - Hệ thống tự động xác thực Email từ Google 100%.<br>- Nếu là Giáo viên mới: Tạo tài khoản `Pending` chờ Admin duyệt.<br>- Nếu đã được duyệt: Đăng nhập thành công vào `/classrooms`. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-02** | **Đăng ký Giáo viên Thủ công & Xác thực OTP 6 số** | **02.1 (Positive)**: Đăng ký thủ công -> Nhập OTP 6 số từ Email | 1. Mở `/register`, chọn vai trò Giáo viên<br>2. Nhập Họ tên, Email, Mật khẩu<br>3. Bấm "Đăng ký"<br>4. Nhập mã OTP 6 số từ Email | Email: `thaya@gmail.com`<br>Pass: `123456` | - Modal OTP 6 ô xuất hiện.<br>- Nhập đúng OTP: Xác thực thành công, tài khoản chuyển sang `Pending` chờ Admin duyệt.<br>- Đăng nhập khi chưa được duyệt: Toast *"Tài khoản của bạn đang chờ Ban giám hiệu phê duyệt..."*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.2 (UI/UX)**: Giao diện Modal OTP 6 ô nhập số | 1. Mở Modal OTP 6 số<br>2. Gõ từng số: kiểm tra tự nhảy con trỏ<br>3. Gõ lại số cũ: kiểm tra bôi đen ghi đè<br>4. Dán 6 số Ctrl+V | Mã OTP 6 chữ số | - 6 ô nhập số riêng biệt `[1][2][3]-[4][5][6]`.<br>- Tự động nhảy con trỏ sang ô kế tiếp khi nhập.<br>- Gõ lại: Bôi đen số cũ và ghi đè tự động.<br>- Dán Ctrl+V 6 số: Tự động điền vào cả 6 ô. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.3 (UI/UX)**: Đếm ngược 30s Gửi lại mã OTP | 1. Mở Modal OTP 6 số<br>2. Quan sát bộ đếm ngược 30s<br>3. Hết 30s bấm "Gửi lại mã" | Re-send OTP click | Bộ đếm ngược chạy từ 30s về 0s. Khi hết 30s nút "Gửi lại mã" sáng lên, click gửi thành công mã OTP mới. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.4 (Negative)**: Nhập sai mã OTP | 1. Nhập OTP ngẫu nhiên sai 6 số<br>2. Bấm Xác nhận | OTP: `999999` | Hiển thị Toast lỗi: *"Mã OTP không hợp lệ hoặc đã hết hạn!"*. Tài khoản không được kích hoạt. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.5 (Positive)**: Đăng ký lại mượt mà cho Email chưa xác thực | 1. Đăng ký với email `draft@gmail.com`<br>2. Tắt Modal OTP (chưa xác thực)<br>3. Thực hiện Đăng ký lại với Email `draft@gmail.com` | Email: `draft@gmail.com` | Hệ thống tự động xóa bản ghi chưa xác thực cũ, phát hành OTP mới mà không báo lỗi trùng Email. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.6 (Negative)**: Đăng ký bằng Email đã tồn tại (đã xác thực) | 1. Đăng ký với email `teacher@gmail.com` (đã có trong hệ thống) | Email: `teacher@gmail.com` | Hiển thị Toast lỗi: *"Email này đã được đăng ký!"*. Không cho phép tạo trùng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.7 (Security)**: Tự động bật Modal OTP khi cố Đăng nhập tài khoản chưa xác thực | 1. Đăng ký tài khoản nhưng tắt Modal OTP<br>2. Mở trang `/login` và bấm Đăng nhập | Email chưa xác thực | Hiển thị Toast cảnh báo và **tự động mở Modal OTP 6 số ngay trên màn hình Đăng nhập** để kích hoạt tại chỗ. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.8 (Validation)**: Nhập Email sai định dạng khi Đăng ký | 1. Nhập Email không có `@` hoặc thiếu tên miền<br>2. Bấm "Đăng ký" | Email: `teacherbadformat` | Hiển thị lỗi validation: *"Email không đúng định dạng!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **02.9 (Boundary)**: Mật khẩu không đủ mạnh (< 8 ký tự hoặc thiếu chữ hoa/chữ thường/chữ số/ký tự đặc biệt) | 1. Nhập Mật khẩu không đủ mạnh (quá ngắn hoặc thiếu ký tự quy định)<br>2. Bấm "Đăng ký" | Pass: `123` hoặc `Password123` | Hiển thị lỗi validation: *"Mật khẩu phải chứa ít nhất 8 ký tự, bao gồm cả chữ hoa, chữ thường, chữ số và ký tự đặc biệt!"*. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-03** | **Quản lý & Cập nhật Hồ sơ Cá nhân (`/profile`)** | **03.1 (View)**: Xem đầy đủ thông tin cá nhân | 1. Truy cập `/profile` | Profile Data | Hiển thị đầy đủ: Avatar, Họ tên, Email, Giới tính, Ngày sinh, SĐT/Zalo, Bằng cấp/Trình độ, Môn giảng dạy, Bio giới thiệu, Ngày đăng ký. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03.2 (Positive)**: Chỉnh sửa thông tin cá nhân | 1. Click "Chỉnh sửa hồ sơ"<br>2. Cập nhật Họ tên, SĐT, Bằng cấp, Bio<br>3. Bấm "Lưu" | New Profile Info | Toast "Cập nhật hồ sơ thành công", dữ liệu trên giao diện được làm mới tức thì. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03.2B (Validation)**: Chỉnh sửa thông tin không hợp lệ (tên rỗng/chữ số, SĐT sai định dạng, ngày sinh tương lai) | 1. Click "Chỉnh sửa hồ sơ"<br>2. Nhập tên chứa số, SĐT thiếu số hoặc ngày sinh tương lai<br>3. Bấm "Lưu" | Tên: `A123`, SĐT: `123` | Hiển thị Toast thông báo lỗi validation tương ứng từ hệ thống. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03.3 (Positive)**: Đổi Avatar hợp lệ (< 2MB) | 1. Click biểu tượng bút ở Avatar<br>2. Chọn file `.jpg` / `.png` < 2MB | Valid Image File | Tải ảnh lên thành công, Avatar ở Header và Profile tự động đổi sang ảnh mới. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03.4 (Negative)**: Đổi Avatar quá dung lượng (> 5MB) hoặc sai định dạng | 1. Chọn file ảnh > 5MB hoặc file `.pdf` | Invalid File | Hiển thị thông báo lỗi: *"Dung lượng file vượt quá giới hạn 2MB!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03.5 (Validation)**: Ràng buộc chưa đủ thông tin trước khi Tạo Lớp | 1. Tạo tài khoản Giáo viên mới chưa điền SĐT/Bằng cấp<br>2. Vào `/classrooms` bấm "Tạo lớp học mới" | Account thiếu thông tin | - Xuất hiện Modal Cảnh Báo *"Cần hoàn thiện hồ sơ trước khi tạo lớp"*.<br>- Liệt kê chính xác trường thông tin còn thiếu.<br>- Bấm **[ Cập nhật hồ sơ ngay ]** tự động chuyển hướng đến `/profile`. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-03B** | **Đổi mật khẩu & Thiết lập mật khẩu Giáo viên** | **03B.1 (Positive)**: Nhập đúng Mật khẩu cũ & Mật khẩu mới chuẩn (Tài khoản thường) | 1. Vào `/profile` -> Click "Đổi mật khẩu"<br>2. Nhập Pass cũ `teacher123`<br>3. Nhập Pass mới `teacher@2026`<br>4. Nhập lại Pass mới xác nhận<br>5. Bấm "Xác nhận" | Pass cũ: `teacher123`<br>Pass mới: `teacher@2026` | Toast *"Đổi mật khẩu thành công"*. Đăng xuất và dùng `teacher@2026` đăng nhập lại thành công. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03B.2 (Negative)**: Nhập sai Mật khẩu hiện tại (Tài khoản thường) | 1. Nhập sai Mật khẩu hiện tại<br>2. Bấm "Xác nhận" | Pass cũ: `wrongoldpass` | Báo lỗi: *"Mật khẩu hiện tại không chính xác!"*. Mật khẩu không đổi. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03B.3 (Negative)**: Mật khẩu mới và Mật khẩu xác nhận không khớp | 1. Pass mới và Pass xác nhận khác nhau | Pass mới: `abc123`<br>Xác nhận: `def456` | Báo lỗi: *"Mật khẩu xác nhận không khớp!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03B.4 (Positive - Google OAuth)**: Thiết lập mật khẩu liên kết lần đầu qua OTP Email | 1. Đăng nhập qua Google -> Vào `/profile`<br>2. Click "Đổi mật khẩu" (Thiết lập mật khẩu)<br>3. Bấm "Nhận mã OTP" để gửi mã về email<br>4. Nhập OTP chính xác từ Email, nhập Pass mới & xác nhận<br>5. Bấm "Xác nhận thiết lập" | OTP từ Email<br>Pass mới: `teacher@2026` | - Toast *"Thiết lập mật khẩu thành công!"*.<br>- Tài khoản được chuyển thành tài khoản hỗn hợp, có thể đăng nhập bằng cả Google và Email/Mật khẩu. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03B.5 (Negative - Google OAuth)**: Nhập sai mã OTP thiết lập mật khẩu | 1. Thực hiện thiết lập mật khẩu<br>2. Nhập sai mã OTP hoặc mã hết hạn<br>3. Bấm "Xác nhận thiết lập" | OTP: `000000` | Báo lỗi: *"Mã OTP không chính xác!"*. Mật khẩu không đổi. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-03C** | **Đăng xuất hệ thống** | **03C.1 (Positive)**: Thực hiện Đăng xuất | 1. Click Avatar ở Header<br>2. Click "Đăng xuất" | Action Logout | Xóa Token lưu vết và chuyển hướng tự động về `/login`. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-03D** | **Bảo vệ Route & Phân quyền Giáo viên** | **03D.1 (Security)**: Giáo viên cố truy cập URL Admin | 1. Đăng nhập Teacher<br>2. Gõ URL `/admin/dashboard` hoặc `/admin/users` | Teacher Account | Tự động chặn và chuyển hướng về `/classrooms`. Không rò rỉ dữ liệu trang Admin. | `[ ] Pass`<br>`[ ] Fail` |
| | | **03D.2 (Security)**: Người dùng chưa đăng nhập (Guest) truy cập URL Giáo viên | 1. Mở tab ẩn danh (Guest)<br>2. Gõ URL `http://localhost:5173/classrooms` | Guest | Tự động chuyển hướng về `/login`. | `[ ] Pass`<br>`[ ] Fail` |

---

### MODULE 2: QUẢN LÝ LỚP HỌC & TRẠNG THÁI LỚP (`/classrooms`)

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-04** | **Tạo Lớp học Mới & Tự động sinh classCode** | **04.1 (Positive)**: Tạo lớp mới hợp lệ | 1. Đăng nhập Giáo viên đủ hồ sơ<br>2. Click nút "Tạo lớp học mới" (`AnimatedAddButton`)<br>3. Nhập Tên lớp & Môn học<br>4. Bấm "Tạo lớp ngay" | Tên: `Toán 12A1`<br>Môn: `Toán` | - Hệ thống tự động sinh `classCode` 6 ký tự ngẫu nhiên duy nhất (VD: `M8K9L2`).<br>- Lớp mới có trạng thái `Pending` (Màu cam có hiệu ứng nhấp nháy).<br>- Tự động được ưu tiên xếp ở **TOP ĐẦU Trang 1** Dashboard Giáo viên.<br>- Toast thông báo *"Tạo lớp học thành công!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **04.2 (Validation)**: Để trống Tên lớp hoặc Môn học | 1. Để trống Tên lớp<br>2. Bấm "Tạo lớp ngay" | Form thiếu Tên lớp | Hiển thị Toast lỗi: *"Vui lòng điền đầy đủ thông tin tên lớp và môn học!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **04.3 (Security)**: Cố nhấp chuột vào lớp đang Pending | 1. Click vào thẻ lớp `Pending` từ Dashboard | Click Pending Class | Chặn không cho truy cập vào chi tiết lớp, hiện Toast: *"Lớp học đang chờ Admin duyệt, chưa thể truy cập."*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **04.4 (Boundary)**: Tên lớp rất dài (> 100 ký tự) | 1. Nhập tên lớp > 100 ký tự<br>2. Bấm "Tạo lớp ngay" | Tên lớp dài 120 chars | Hệ thống cắt bớt hoặc hiển thị lỗi validation giới hạn ký tự. Giao diện không bị vỡ layout. | `[ ] Pass`<br>`[ ] Fail` |
| | | **04.5 (Security)**: Nhập ký tự HTML/XSS vào Tên lớp | 1. Nhập `<script>alert('xss')</script>` vào Tên lớp<br>2. Bấm "Tạo lớp ngay" | XSS Payload | Hệ thống tự động escape ký tự đặc biệt, không thực thi mã JavaScript. Tên lớp hiển thị dạng text thuần. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-05** | **Chế độ Xem Lưới & Bảng (Grid & Table View)** | **05.1 (UI/UX)**: Chuyển đổi qua lại giữa dạng Lưới và Bảng | 1. Click icon Chuyển xem Lưới/Bảng ở góc trên bên phải | Switch View Icon | Giao diện chuyển đổi mượt mà giữa dạng Thẻ Lưới (Grid) và Bảng Tính (Table), dữ liệu đồng bộ 100%. | `[ ] Pass`<br>`[ ] Fail` |
| | | **05.2 (Data Consistency)**: Dữ liệu đồng bộ giữa 2 chế độ xem | 1. Xem dạng Grid, ghi nhận số lớp và thông tin<br>2. Chuyển sang dạng Table<br>3. So sánh dữ liệu | Grid vs Table | Số lượng lớp, tên, mã code, sĩ số, trạng thái hoàn toàn khớp giữa 2 chế độ hiển thị. | `[ ] Pass`<br>`[ ] Fail` |
| | | **05.3 (Search)**: Tìm kiếm lớp học theo tên/mã code | 1. Nhập từ khóa tên lớp hoặc mã code vào ô tìm kiếm | Search: `Toán` hoặc `M8K9L2` | Danh sách lớp tự động lọc chính xác theo từ khóa tìm kiếm. | `[ ] Pass`<br>`[ ] Fail` |
| | | **05.4 (Filter)**: Lọc theo trạng thái lớp (Active / Pending / Closed / Locked) | 1. Chọn Dropdown Lọc trạng thái<br>2. Chọn `Hoạt động`, `Chờ duyệt`, `Đã đóng`, hoặc `Bị khóa` | Filter Status | Danh sách lớp chỉ hiển thị các lớp thuộc đúng trạng thái được chọn. | `[ ] Pass`<br>`[ ] Fail` |
| | | **05.5 (Empty State)**: Tìm kiếm không có kết quả | 1. Nhập chuỗi ngẫu nhiên không khớp | Search: `xyz123456` | Hiển thị trạng thái trống thân thiện: *"Không tìm thấy lớp học phù hợp"*. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-06** | **Đóng & Mở lại Lớp học (Close/Re-open)** | **06.1 (Positive)**: Đóng lớp học sau khi kết thúc kỳ | 1. Tại Thẻ lớp hoặc Menu `...` -> Chọn "Đóng lớp học"<br>2. Xác nhận trong Dialog | Active Class | - Trạng thái đổi thành `Đã đóng` (`Closed`), thẻ mờ đi (`opacity: 0.6`, `cursor: not-allowed`).<br>- Cả Giáo viên và Học sinh bấm vào đều bị chặn + Toast: *"Lớp học đã bị đóng, không thể truy cập."*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **06.2 (Positive)**: Mở lại lớp học | 1. Bấm nút "Mở lại lớp" trên thẻ hoặc Menu `...` | Closed Class | Trạng thái chuyển lại thành `Active` (Màu xanh), Giáo viên và Học sinh lại truy cập tương tác bình thường. | `[ ] Pass`<br>`[ ] Fail` |
| | | **06.3 (Impact Check)**: Học sinh cố truy cập lớp đã đóng | 1. Đăng nhập tài khoản Học sinh<br>2. Click vào lớp có trạng thái `Closed` | Student -> Closed Class | Hệ thống chặn truy cập, hiển thị Toast cảnh báo. Học sinh không thể nộp bài hay tương tác mới. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-07** | **Lưu trữ Lớp học (Archive Class)** | **07.1 (Positive)**: Đưa lớp vào kho Lưu trữ | 1. Menu `...` -> Chọn "Lưu trữ lớp"<br>2. Bấm Xác nhận trong Dialog | Select Class | Toast thông báo lưu trữ thành công, lớp bị ẩn khỏi Dashboard (Soft delete) nhưng bảo lưu trọn vẹn dữ liệu. | `[ ] Pass`<br>`[ ] Fail` |
| | | **07.2 (Verify)**: Xác nhận lớp đã ẩn khỏi Dashboard | 1. Quay lại Dashboard<br>2. Kiểm tra danh sách lớp | Dashboard Check | Lớp vừa lưu trữ không còn xuất hiện trong danh sách lớp. Dữ liệu vẫn còn trong Database. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-08** | **Nhận Thông báo Real-time khi Admin Khóa/Mở khóa** | **08.1 (Real-time Socket)**: Admin khóa lớp -> Giáo viên nhận thông báo tức thì | 1. Mở song song 2 trình duyệt (Admin & Teacher)<br>2. Admin bấm Khóa 1 lớp của Giáo viên | Admin Lock Action | - Quả chuông trên Header Giáo viên **nảy chấm đỏ real-time**.<br>- Click Chuông xem thông báo: *"Lớp học [Tên lớp] của bạn đã bị Quản trị viên hệ thống khóa."*<br>- Thẻ lớp trên Dashboard Giáo viên tự động chuyển sang `LOCKED` mờ đi mà không cần F5. | `[ ] Pass`<br>`[ ] Fail` |
| | | **08.2 (Real-time Socket)**: Admin mở khóa lớp -> Giáo viên nhận thông báo | 1. Admin bấm "Mở khóa lớp"<br>2. Quan sát Dashboard Giáo viên | Admin Unlock Action | Thẻ lớp tự động chuyển lại `Active`, thông báo mới xuất hiện trên Chuông. | `[ ] Pass`<br>`[ ] Fail` |
| | | **08.3 (Security)**: Giáo viên cố truy cập lớp bị Admin khóa | 1. Giáo viên click vào thẻ lớp đang `LOCKED` | Locked Class by Admin | Chặn hoàn toàn, hiển thị Toast cảnh báo: *"Lớp học này đã bị khóa bởi Quản trị viên hệ thống!"*. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-09** | **Sao chép Mã Lớp 1-Touch & Chỉnh sửa Lớp** | **09.1 (Positive)**: Nút sao chép mã code 1 chạm | 1. Click nút Copy bên cạnh mã lớp `classCode` | Copy Button Click | Toast thông báo *"Đã sao chép mã lớp vào bộ nhớ tạm!"*. Dán ra văn bản thu được đúng mã code. | `[ ] Pass`<br>`[ ] Fail` |
| | | **09.2 (Positive)**: Chỉnh sửa thông tin Lớp học | 1. Menu `...` -> "Chỉnh sửa lớp"<br>2. Sửa Tên lớp hoặc Mô tả<br>3. Bấm "Lưu thay đổi" | New Class Name | Thông tin lớp học được cập nhật thành công, Toast xác nhận lưu thành công. | `[ ] Pass`<br>`[ ] Fail` |
| | | **09.3 (Validation)**: Chỉnh sửa Tên lớp thành rỗng | 1. Xóa hết Tên lớp<br>2. Bấm "Lưu thay đổi" | Tên lớp rỗng | Hiển thị lỗi validation: *"Tên lớp không được để trống!"*. Không cho lưu. | `[ ] Pass`<br>`[ ] Fail` |

---

### MODULE 3: KHÔNG GIAN LỚP HỌC, QUẢN LÝ HỌC SINH & BẢNG TIN (`/classrooms/:id`, `/classrooms/:id/students`)

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-10** | **Phê duyệt Yêu cầu Xin vào Lớp của Học sinh** | **10.1 (Positive)**: Duyệt đơn lẻ 1 học sinh | 1. Bấm nút "Duyệt học sinh" trên thẻ/bảng lớp<br>2. Tại hàng học sinh -> Bấm "Duyệt" | Pending Student Request | - Học sinh được duyệt chuyển sang trạng thái chính thức trong lớp.<br>- Sĩ số lớp tự động tăng lên. | `[ ] Pass`<br>`[ ] Fail` |
| | | **10.2 (Positive)**: Duyệt tất cả (`Approve All`) | 1. Bấm nút "Duyệt tất cả" khi có nhiều đơn xin vào lớp | Multiple Pending Requests | Toàn bộ học sinh chờ duyệt được chấp nhận cùng lúc. Sĩ số lớp cập nhật đúng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **10.3 (Positive)**: Từ chối học sinh xin vào lớp | 1. Bấm nút "Từ chối" ở hàng học sinh xin vào lớp | Reject Action | Đơn xin gia nhập của học sinh bị hủy bỏ, học sinh nhận được thông báo từ chối. | `[ ] Pass`<br>`[ ] Fail` |
| | | **10.4 (Edge Case)**: Không có yêu cầu nào chờ duyệt | 1. Mở Panel duyệt học sinh khi không có đơn nào | No Pending | Hiển thị thông báo: *"Không có yêu cầu nào đang chờ duyệt"*. Nút "Duyệt tất cả" bị vô hiệu hóa. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-11** | **Quản lý Danh sách Học sinh trong Lớp** | **11.1 (Positive)**: Thêm trực tiếp Học sinh vào lớp bằng Email | 1. Vào Tab Thành viên -> Bấm "Thêm học sinh"<br>2. Nhập Email học sinh đã có trên hệ thống<br>3. Bấm "Thêm" | Email: `student@gmail.com` | Học sinh được thêm trực tiếp vào lớp mà không cần qua bước duyệt `Pending`. Sĩ số tăng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **11.2 (Negative)**: Thêm học sinh bằng Email không tồn tại trên hệ thống | 1. Nhập Email chưa đăng ký: `notexist@gmail.com`<br>2. Bấm "Thêm" | Email: `notexist@gmail.com` | Hiển thị Toast lỗi: *"Không tìm thấy tài khoản học sinh với email này!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **11.3 (Negative)**: Thêm học sinh đã có trong lớp | 1. Nhập Email học sinh đã thuộc lớp<br>2. Bấm "Thêm" | Email học sinh đã có | Hiển thị Toast cảnh báo: *"Học sinh này đã có trong lớp học!"*. Không tạo trùng lặp. | `[ ] Pass`<br>`[ ] Fail` |
| | | **11.4 (Positive)**: Mời học sinh ra khỏi lớp (Xóa khỏi lớp) | 1. Tại hàng học sinh -> Bấm Menu `...` -> Chọn "Xóa khỏi lớp"<br>2. Bấm Xác nhận trong AlertDialog | Select Student | Học sinh bị xóa khỏi danh sách lớp, không còn truy cập vào bài tập của lớp đó được nữa. Sĩ số giảm. | `[ ] Pass`<br>`[ ] Fail` |
| | | **11.5 (View)**: Xem danh sách đầy đủ thành viên | 1. Vào Tab Thành viên | Member List | Hiển thị danh sách học sinh kèm: Avatar, Họ tên, Email, Ngày tham gia, Trạng thái. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-12** | **Đăng Thông báo & Chia sẻ Tài liệu Bảng tin** | **12.1 (Positive)**: Đăng thông báo dạng text thuần | 1. Vào Tab Bảng tin -> Nhập nội dung thông báo<br>2. Bấm "Đăng bài" | Text: `Tuần sau thi giữa kỳ` | Bài đăng xuất hiện trên Bảng tin công khai của lớp, hiển thị tên giáo viên và thời gian đăng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **12.2 (Positive)**: Đăng thông báo kèm file PDF/Word | 1. Nhập nội dung thông báo<br>2. Đính kèm file PDF hoặc Word<br>3. Bấm "Đăng bài" | Text & File PDF/Word | Bài đăng hiển thị kèm biểu tượng file đính kèm. Học sinh có thể click tải file. | `[ ] Pass`<br>`[ ] Fail` |
| | | **12.3 (Positive)**: Đăng thông báo kèm Link Youtube/Drive | 1. Nhập nội dung chứa link Youtube<br>2. Bấm "Đăng bài" | Text & Link Video | Link Youtube hiển thị dạng nhúng (embed) hoặc hyperlink có thể click xem. | `[ ] Pass`<br>`[ ] Fail` |
| | | **12.4 (Validation)**: Đăng bài trống nội dung | 1. Không nhập gì<br>2. Bấm "Đăng bài" | Form trống | Hiển thị cảnh báo: *"Vui lòng nhập nội dung thông báo!"*. Không cho đăng bài trống. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-12B** | **Bình luận & Quản lý bình luận công khai** | **12B.1 (Positive)**: Giáo viên gõ bình luận trả lời Học sinh | 1. Gõ câu trả lời bên dưới bài đăng<br>2. Bấm gửi bình luận | Comment text | Bình luận xuất hiện tức thì bên dưới bài đăng kèm avatar, tên và thời gian. | `[ ] Pass`<br>`[ ] Fail` |
| | | **12B.2 (Positive)**: Giáo viên xóa bình luận không phù hợp | 1. Bấm nút xóa bên cạnh bình luận<br>2. Xác nhận xóa | Delete Comment | Bình luận biến mất khỏi Bảng tin. | `[ ] Pass`<br>`[ ] Fail` |
| | | **12B.3 (Validation)**: Gửi bình luận trống | 1. Không nhập nội dung<br>2. Bấm gửi bình luận | Empty Comment | Hệ thống không cho gửi bình luận trống, hiển thị cảnh báo hoặc disable nút gửi. | `[ ] Pass`<br>`[ ] Fail` |

---

### MODULE 4: TẠO BÀI TẬP TỰ LUẬN & CHẤM ĐIỂM (`/assignments`)

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-13** | **Giao Bài tập Tự luận Mới** | **13.1 (Positive)**: Tạo bài tập tự luận có hạn nộp & hệ số | 1. Tab Bài tập -> Bấm "Giao bài tập mới" (`AnimatedAddButton`)<br>2. Điền Tiêu đề, Mô tả, Chọn Hạn nộp (Deadline), Hệ số điểm<br>3. Bấm nút giao bài | Title: `Bài tập chương 1`<br>Deadline: Ngày mai<br>Score Weight: `1` | - Bài tập được giao thành công, xuất hiện Toast xác nhận.<br>- Thông báo bài tập mới tự động phát tới toàn bộ Học sinh trong lớp.<br>- Bài tập hiển thị trên Tab Bài tập với trạng thái `Chưa chấm`. | `[ ] Pass`<br>`[ ] Fail` |
| | | **13.2 (Validation)**: Để trống Tiêu đề bài tập | 1. Không nhập Tiêu đề<br>2. Bấm giao bài | Missing Title | Hiển thị lỗi validation: *"Vui lòng nhập tiêu đề bài tập!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **13.3 (Boundary)**: Chọn Deadline trong quá khứ | 1. Chọn ngày hạn nộp là ngày hôm qua<br>2. Bấm giao bài | Past Deadline | Hệ thống cảnh báo hoặc không cho phép đặt hạn nộp trong quá khứ. | `[ ] Pass`<br>`[ ] Fail` |
| | | **13.4 (Positive)**: Giao bài tập kèm file tài liệu đính kèm | 1. Điền thông tin bài tập<br>2. Đính kèm file PDF/Word hướng dẫn<br>3. Bấm giao bài | Title + File PDF | Bài tập hiển thị kèm link tải file tài liệu hướng dẫn cho Học sinh. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-14** | **Chấm bài Tự luận & Lời phê** | **14.1 (Positive)**: Xem bài nộp -> Nhập điểm & Lời phê | 1. Mở danh sách bài nộp của bài tập<br>2. Click xem file đính kèm PDF/Word của học sinh<br>3. Nhập điểm `9.5` & gõ Lời phê nhận xét<br>4. Bấm "Lưu điểm" | Score: `9.5`<br>Lời phê: `Bài làm xuất sắc` | - Điểm số và lời phê lưu thành công, học sinh nhận được thông báo điểm.<br>- Dữ liệu điểm **tự động đồng bộ 100% sang Sổ điểm (`/gradebook`)**. | `[ ] Pass`<br>`[ ] Fail` |
| | | **14.2 (Boundary)**: Nhập điểm vượt thang điểm tối đa | 1. Nhập điểm `11` khi thang điểm 10<br>2. Bấm "Lưu điểm" | Score: `11` (vượt max) | Hiển thị cảnh báo hoặc tự động giới hạn điểm tối đa. Không cho lưu điểm vượt ngưỡng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **14.3 (Boundary)**: Nhập điểm âm | 1. Nhập điểm `-1`<br>2. Bấm "Lưu điểm" | Score: `-1` | Hệ thống từ chối, hiển thị lỗi validation: *"Điểm không được âm!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **14.4 (Positive)**: Chỉnh sửa điểm đã chấm | 1. Mở bài đã chấm điểm<br>2. Sửa điểm từ `9.5` thành `8.0`<br>3. Bấm "Lưu điểm" | Score: `8.0` | Điểm mới ghi đè thành công. Sổ điểm đồng bộ giá trị mới. | `[ ] Pass`<br>`[ ] Fail` |
| | | **14.5 (Edge Case)**: Xem bài tập chưa có học sinh nộp | 1. Mở danh sách bài nộp khi chưa có bài nộp nào | No Submissions | Hiển thị thông báo thân thiện: *"Chưa có học sinh nào nộp bài"*. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-14B** | **Thảo luận với Học sinh trong Bài tập** | **14B.1 (Positive)**: Giáo viên nhắn tin trao đổi trong khung Thảo luận | 1. Mở chi tiết bài tập của 1 Học sinh<br>2. Gõ tin nhắn trong khung "Thảo luận với Giáo viên"<br>3. Bấm gửi | Message: `Em kiểm tra lại bài nhé` | Tin nhắn xuất hiện trong khung thảo luận. Học sinh nhìn thấy tin nhắn khi mở bài tập. | `[ ] Pass`<br>`[ ] Fail` |

---

### MODULE 5: NGÂN HÀNG ĐỀ & TẠO ĐỀ THI AI GEMINI (`/bank`)

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-15** | **Xem Danh sách Ngân hàng Đề Giáo viên** | **15.1 (View List)**: Truy cập danh sách học liệu cá nhân | 1. Đăng nhập Giáo viên<br>2. Truy cập `/bank` | Danh sách học liệu | Hiển thị bảng/lưới tài nguyên đề thi & bài tập của Giáo viên. Mỗi item hiển thị: Tiêu đề, Loại (Trắc nghiệm/Tự luận), Môn học, Ngày tạo, Trạng thái. | `[ ] Pass`<br>`[ ] Fail` |
| | | **15.2 (Filter Types)**: Lọc học liệu theo loại (Quiz / Assignment) | 1. Chọn Tab/Lọc "Trắc nghiệm" hoặc "Tự luận" | Filter Type | Danh sách tự động lọc đúng loại tài nguyên học liệu tương ứng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **15.3 (Filter Subject)**: Lọc theo Môn học | 1. Click bộ lọc Môn học<br>2. Chọn một môn chuẩn (*Toán, Ngữ văn, Tiếng Anh...*) | Môn chọn sẵn | Danh sách hiển thị chính xác các đề thi thuộc môn học đã chọn. | `[ ] Pass`<br>`[ ] Fail` |
| | | **15.4 (Search)**: Tìm kiếm học liệu theo tên | 1. Nhập từ khóa tên học liệu vào ô tìm kiếm | Search: `Chương 1` | Danh sách lọc chính xác theo từ khóa. | `[ ] Pass`<br>`[ ] Fail` |
| | | **15.5 (Empty State)**: Giáo viên mới chưa có học liệu nào | 1. Đăng nhập Giáo viên mới<br>2. Truy cập `/bank` | No Data | Hiển thị trạng thái trống thân thiện hướng dẫn Giáo viên tạo học liệu đầu tiên. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-16** | **Xem Chi tiết Học liệu (Modal Xem chi tiết)** | **16.1 (View Detail)**: Mở modal xem chi tiết đề trắc nghiệm | 1. Click nút "Xem chi tiết" tại 1 đề trắc nghiệm | Select Quiz Item | Modal hiển thị đầy đủ: Tiêu đề, Loại, Môn học, Phạm vi chia sẻ, Điểm tối đa, Thời gian, Mô tả chi tiết, Danh sách câu hỏi kèm đáp án A/B/C/D. | `[ ] Pass`<br>`[ ] Fail` |
| | | **16.2 (Scroll)**: Cuộn danh sách câu hỏi dài | 1. Mở modal chi tiết đề thi có > 10 câu hỏi<br>2. Cuộn xuống kiểm tra câu cuối cùng | Long Quiz (20 câu) | Thanh cuộn hoạt động mượt mà, câu hỏi cuối cùng hiển thị đầy đủ không bị cắt khuất. | `[ ] Pass`<br>`[ ] Fail` |
| | | **16.3 (View Detail)**: Xem chi tiết bài tập tự luận kèm file | 1. Click "Xem chi tiết" tại 1 bài tập dạng file | Select Document Item | Modal hiển thị thông tin chung và nút "Tải xuống tài liệu" hoạt động đúng. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-17** | **Tạo Đề thi Trắc nghiệm Thủ công** | **17.1 (Positive)**: Soạn đề thi từ đầu bằng Builder | 1. Bấm "Tạo đề trắc nghiệm mới"<br>2. Chọn Môn học<br>3. Nhập Tiêu đề, Thời gian làm bài, Điểm tối đa<br>4. Thêm từng câu hỏi: Nhập nội dung, 4 đáp án A/B/C/D, chọn đáp án đúng<br>5. Bấm "Lưu đề thi" | Quiz Data | Đề thi lưu vào Ngân hàng thành công. Hiển thị trong danh sách với đầy đủ thông tin. | `[ ] Pass`<br>`[ ] Fail` |
| | | **17.2 (Validation)**: Câu hỏi chưa có đáp án đúng | 1. Thêm câu hỏi nhưng không chọn đáp án đúng<br>2. Bấm "Lưu đề thi" | Missing Correct Answer | Hiển thị cảnh báo: *"Có câu hỏi chưa chọn đáp án đúng!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **17.3 (Validation)**: Đề thi không có câu hỏi nào | 1. Chỉ nhập tiêu đề, không thêm câu hỏi nào<br>2. Bấm "Lưu đề thi" | No Questions | Hiển thị cảnh báo: *"Vui lòng thêm ít nhất 1 câu hỏi!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **17.4 (Positive)**: Xóa câu hỏi trong khi soạn | 1. Thêm 5 câu hỏi<br>2. Click nút xóa ở câu hỏi thứ 3 | Delete Q3 | Câu 3 biến mất, danh sách tự động đánh lại số thứ tự. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-18** | **Chọn Môn học Mở rộng & `+ Môn khác...`** | **18.1 (Standard Subjects)**: Chọn môn học tiêu chuẩn hệ thống | 1. Tại màn tạo đề -> Click Dropdown chọn Môn học<br>2. Chọn 1 môn (*Toán, Ngữ văn, Tiếng Anh, Vật lý, Hóa học, Sinh học, Lịch sử, Địa lý, Tin học, GDCD, Âm nhạc, Mỹ thuật, Thể dục*) | Môn chọn sẵn | Môn học được gán cho đề thi. Hiển thị đúng trên badge/thẻ. | `[ ] Pass`<br>`[ ] Fail` |
| | | **18.2 (Custom Subject)**: Chọn `+ Môn khác...` và nhập môn mới | 1. Chọn `+ Môn khác...`<br>2. Nhập tên môn: `Tiếng Pháp`<br>3. Xác nhận | Môn khác: `Tiếng Pháp` | Ô nhập tên môn xuất hiện, cho phép nhập tự do. Đề thi lưu với môn học vừa nhập. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-19** | **Xem Hướng dẫn & Tải File Mẫu Word/Excel** | **19.1 (Guide Modal)**: Mở Modal Hướng dẫn định dạng tệp | 1. Tại trang tạo đề, click nút "File mẫu & Hướng dẫn" | User Click | Modal Hướng dẫn mở ra, hiển thị bảng cấu trúc 6 cột Excel và cấu trúc văn bản Word mẫu rõ ràng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **19.2 (Download Samples)**: Tải tệp mẫu Word `.docx` và Excel `.xlsx` | 1. Click nút "Tải file Excel mẫu"<br>2. Click nút "Tải file Word mẫu" | Action Click | Trình duyệt tự động tải xuống tệp mẫu thành công. File mở được và hiển thị cấu trúc mẫu đúng. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-20** | **Import Đề thi từ File (Word / Excel / AI Gemini)** | **20.1 (Excel Import)**: Nhập đề thi từ tệp Excel chuẩn 6 cột | 1. Click "Nhập dữ liệu (Word/Excel)"<br>2. Chọn tệp `.xlsx` mẫu chuẩn | File Excel 6 cột | Hệ thống đọc tệp và import toàn bộ câu hỏi, 4 phương án A/B/C/D và chỉ định đúng đáp án đúng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **20.2 (Word Regex Import)**: Nhập đề thi từ tệp Word chuẩn cấu trúc | 1. Chọn tệp Word `.docx` theo cấu trúc `Câu 1: ...` | File `.docx` chuẩn | Hệ thống tự động bóc tách đúng tên câu hỏi, lựa chọn A, B, C, D và vị trí đáp án đúng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **20.3 (AI Gemini Import)**: Upload file Word bóc tách tự động bằng AI | 1. Click "Tạo đề bằng AI"<br>2. Chọn tệp Word bài giảng lý thuyết bất kỳ | File Word lý thuyết | - AI Gemini (`gemini-2.5-flash`) tự đọc hiểu và sinh bộ câu hỏi trắc nghiệm chuẩn.<br>- Hiển thị màn hình Xem trước cho phép Giáo viên duyệt/sửa trước khi lưu. | `[ ] Pass`<br>`[ ] Fail` |
| | | **20.4 (Negative)**: Upload file không đúng định dạng (PDF, TXT...) | 1. Chọn file `.pdf` hoặc `.txt` để import | Invalid File Format | Hiển thị Toast lỗi: *"Chỉ hỗ trợ tệp Word (.docx) hoặc Excel (.xlsx)!"*. | `[ ] Pass`<br>`[ ] Fail` |
| | | **20.5 (Edge Case)**: Upload file Word trống không có nội dung | 1. Chọn file `.docx` trống<br>2. Bấm import | Empty File | Hiển thị thông báo: *"Không tìm thấy câu hỏi nào trong tệp!"*. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-21** | **Tự động Chia điểm đều theo Điểm tối đa** | **21.1 (Auto Divide)**: Chia đều điểm trắc nghiệm | 1. Điền Điểm tối đa = `10`<br>2. Click nút "Chia điểm đều" | Điểm tối đa: `10`<br>Số câu: `20` | Số điểm từng câu = `10 / 20 = 0.5` đ/câu. Toast báo thành công. | `[ ] Pass`<br>`[ ] Fail` |
| | | **21.2 (Boundary)**: Chia điểm khi số dư không chia hết | 1. Điểm tối đa `10`, số câu `3`<br>2. Click "Chia điểm đều" | 10 / 3 = 3.33... | Điểm mỗi câu được làm tròn hợp lý (VD: 3.33 đ/câu). Tổng điểm vẫn = 10. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-22** | **Chỉnh sửa Học liệu trong Ngân hàng** | **22.1 (Positive)**: Chỉnh sửa thông tin tài nguyên (Tiêu đề, Môn, Điểm, Thời gian) | 1. Click biểu tượng Bút sửa tại 1 học liệu<br>2. Cập nhật Tiêu đề, Môn học, Thang điểm<br>3. Bấm "Lưu thay đổi" (`SaveButton`) | Updated Data | Thông tin học liệu được cập nhật thành công. Toast xác nhận lưu. | `[ ] Pass`<br>`[ ] Fail` |
| | | **22.2 (Validation)**: Chỉnh sửa Tiêu đề thành rỗng | 1. Xóa hết tiêu đề<br>2. Bấm "Lưu thay đổi" | Empty Title | Hiển thị lỗi: *"Tiêu đề không được để trống!"*. Không cho lưu. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-23** | **Xóa Tài nguyên Học liệu** | **23.1 (Positive - Single Delete)**: Xóa 1 tài nguyên | 1. Click biểu tượng Thùng rác tại 1 học liệu<br>2. Xác nhận trong AlertDialog (nút "Xóa tài nguyên" đỏ) | Select Item | - AlertDialog xác nhận hiện lên (không phải `window.confirm()`).<br>- Bấm "Xóa tài nguyên": Tài nguyên bị xóa vĩnh viễn khỏi Ngân hàng. Toast thành công. | `[ ] Pass`<br>`[ ] Fail` |
| | | **23.2 (Positive - Bulk Delete)**: Chọn nhiều & xóa hàng loạt | 1. Tích chọn checkbox nhiều học liệu<br>2. Bấm nút "Xóa các tài nguyên đã chọn"<br>3. Xác nhận trong AlertDialog | Multiple Items Selected | AlertDialog hiển thị số lượng: *"Xóa X tài nguyên?"*. Bấm xác nhận: Tất cả bị xóa, danh sách cập nhật. | `[ ] Pass`<br>`[ ] Fail` |
| | | **23.3 (UI/UX)**: Hủy xóa trong AlertDialog | 1. Click biểu tượng Thùng rác<br>2. Trong AlertDialog bấm "Hủy" hoặc icon X | Cancel Delete | AlertDialog đóng lại, tài nguyên không bị xóa. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-24** | **Giao đề thi Trắc nghiệm cho Lớp** | **24.1 (Positive)**: Giao đề thi trắc nghiệm có đồng hồ đếm ngược | 1. Chọn đề thi vừa tạo -> Bấm "Giao bài thi cho lớp"<br>2. Chọn Lớp học & Thời gian làm bài (VD: 15 phút, 45 phút)<br>3. Xác nhận giao bài | Timer: 15 mins<br>Class: `Toán 12A1` | Bài thi trắc nghiệm xuất hiện trên giao diện Học sinh kèm đồng hồ tính giờ đếm ngược. | `[ ] Pass`<br>`[ ] Fail` |
| | | **24.2 (Positive)**: Chọn nhiều lớp để giao cùng 1 đề | 1. Chọn đề thi<br>2. Chọn 2-3 lớp học cùng lúc<br>3. Giao bài thi | Multiple Classes | Tất cả lớp được chọn đều nhận đề thi. Học sinh mỗi lớp đều thấy bài thi mới. | `[ ] Pass`<br>`[ ] Fail` |

---

### MODULE 6: ĐIỂM DANH HÀNG NGÀY (`/attendance`)

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-25** | **Điểm danh Lớp học Hàng ngày** | **25.1 (Positive)**: Tích chọn trạng thái chuyên cần mỗi Học sinh | 1. Truy cập `/attendance`<br>2. Chọn Lớp học & chọn Ngày điểm danh<br>3. Tích chọn trạng thái từng HS: `Có mặt`, `Đi muộn`, `Vắng mặt`<br>4. Bấm "Lưu điểm danh" | Attendance Status per Student | - Dữ liệu điểm danh lưu thành công vào CSDL. Toast xác nhận.<br>- Hệ thống tự động tính điểm thưởng XP chuyên cần: Có mặt **+5 XP**, Đi muộn **+2 XP**, Vắng mặt **-5 XP**. | `[ ] Pass`<br>`[ ] Fail` |
| | | **25.2 (Positive)**: Thêm ghi chú lý do vắng cho Học sinh | 1. Click vào ô ghi chú bên cạnh HS vắng mặt<br>2. Nhập lý do: `Ốm`<br>3. Bấm lưu | Note: `Ốm` | Ghi chú lý do vắng lưu thành công, hiển thị khi xem lại lịch sử điểm danh. | `[ ] Pass`<br>`[ ] Fail` |
| | | **25.3 (Positive)**: Chỉnh sửa điểm danh ngày cũ | 1. Chọn ngày đã điểm danh trước đó<br>2. Sửa trạng thái 1 HS từ `Vắng mặt` thành `Có mặt`<br>3. Bấm lưu lại | Update Attendance | Trạng thái mới ghi đè thành công. XP tự động tính lại. | `[ ] Pass`<br>`[ ] Fail` |
| | | **25.4 (View)**: Xem tổng kết tỷ lệ chuyên cần cả lớp | 1. Quan sát bảng thống kê chuyên cần bên dưới | Stats View | Hiển thị tỷ lệ Có mặt, Đi muộn, Vắng mặt dạng phần trăm hoặc biểu đồ. | `[ ] Pass`<br>`[ ] Fail` |
| | | **25.5 (Edge Case)**: Điểm danh lớp không có học sinh nào | 1. Chọn lớp mới tạo chưa có học sinh<br>2. Mở trang điểm danh | Empty Class | Hiển thị thông báo thân thiện: *"Chưa có học sinh trong lớp"*. Không có lỗi. | `[ ] Pass`<br>`[ ] Fail` |

---

### MODULE 7: SỔ ĐIỂM BẢNG TÍNH SPREADSHEET (`/gradebook`)

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-26** | **Nhập điểm trực tiếp trên Bảng tính Spreadsheet** | **26.1 (Spreadsheet Grid)**: Click trực tiếp vào ô điểm để nhập | 1. Truy cập `/gradebook` -> Chọn Lớp<br>2. Click trực tiếp vào ô điểm (Miệng, 15p, 1 tiết, Cuối kỳ)<br>3. Nhập điểm -> Bấm `SaveButton` để lưu | Student Grades | - Hệ thống tự động tính toán Điểm Trung Bình (ĐTB) môn theo đúng công thức hệ số.<br>- Tự động xếp loại học lực Học sinh (Giỏi / Khá / Trung bình / Yếu). | `[ ] Pass`<br>`[ ] Fail` |
| | | **26.2 (Boundary)**: Nhập điểm ngoài khoảng 0-10 | 1. Nhập `12` hoặc `-3` vào ô điểm | Invalid Score | Hệ thống không cho phép nhập hoặc hiển thị cảnh báo. Điểm phải nằm trong khoảng 0 - 10. | `[ ] Pass`<br>`[ ] Fail` |
| | | **26.3 (Boundary)**: Nhập ký tự chữ cái vào ô điểm | 1. Nhập `abc` vào ô điểm | Non-numeric Input | Ô chỉ chấp nhận số, ký tự chữ bị từ chối hoặc bỏ qua. | `[ ] Pass`<br>`[ ] Fail` |
| | | **26.4 (Auto Calculate)**: Kiểm tra công thức tính ĐTB tự động | 1. Nhập đủ các cột điểm: Miệng, 15p, 1 tiết, Cuối kỳ<br>2. Quan sát cột ĐTB | Full Grade Data | ĐTB tính chính xác theo công thức hệ số:<br>ĐTB = (Miệng×1 + 15p×1 + 1Tiết×2 + CuốiKỳ×3) / 7. Xếp loại đúng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **26.5 (Sync Data)**: Đồng bộ điểm từ Bài tập sang Sổ điểm | 1. Chấm điểm bài tập tại Tab Bài tập<br>2. Mở Sổ điểm kiểm tra | Score from Assignment | Điểm vừa chấm ở Tab Bài tập tự động hiển thị trên Sổ điểm tương ứng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **26.6 (Edge Case)**: Sổ điểm lớp chưa có Học sinh | 1. Mở Sổ điểm cho lớp không có Học sinh | Empty Gradebook | Hiển thị thông báo: *"Chưa có học sinh trong lớp"*. Không bị crash. | `[ ] Pass`<br>`[ ] Fail` |

---

### MODULE 8: CHUÔNG THÔNG BÁO & TIỆN ÍCH GIÁO VIÊN

| Mã TC | Tên Kịch Bản | Trường Hợp Kiểm Thử (Sub-Cases) | Các Bước Thực Hiện | Dữ Liệu Đầu Vào | Kết Quả Mong Đợi (Expected Outcome) | Trạng Thái |
| :---: | :--- | :--- | :--- | :--- | :--- | :---: |
| **TC-TCH-27** | **Chuông Thông báo Giáo viên (Notification Popover)** | **27.1 (Real-time)**: Nhận thông báo khi Admin khóa/mở khóa lớp | 1. Admin khóa 1 lớp của Giáo viên<br>2. Giáo viên click Chuông thông báo trên Header | Admin Lock Event | - Chuông xuất hiện chấm đỏ.<br>- Popover hiển thị thông báo: *"Lớp học [Tên lớp] đã bị khóa bởi Quản trị viên."* | `[ ] Pass`<br>`[ ] Fail` |
| | | **27.2 (Real-time)**: Nhận thông báo khi Học sinh nộp bài | 1. Học sinh nộp bài tập<br>2. Giáo viên kiểm tra Chuông | Student Submit Event | Chuông xuất hiện chấm đỏ, thông báo có bài nộp mới từ Học sinh. | `[ ] Pass`<br>`[ ] Fail` |
| | | **27.3 (Interactive)**: Đánh dấu đã đọc thông báo | 1. Click vào 1 thông báo chưa đọc | Click Notification | Thông báo chuyển trạng thái đã đọc, dấu chấm đỏ biến mất nếu đã đọc hết. | `[ ] Pass`<br>`[ ] Fail` |
| **TC-TCH-28** | **Menu Sidebar Giáo viên** | **28.1 (Navigation)**: Chuyển đổi qua lại giữa các menu | 1. Click lần lượt: Lớp học, Ngân hàng đề, Điểm danh, Sổ điểm | Click từng menu item | URL chuyển tương ứng: `/classrooms`, `/bank`, `/attendance`, `/gradebook`. Menu hiện tại được Highlight rõ ràng. | `[ ] Pass`<br>`[ ] Fail` |
| | | **28.2 (UI/UX)**: Thu gọn & Mở rộng Sidebar | 1. Hover vào Sidebar<br>2. Di chuột ra ngoài | Hover / Unhover | Sidebar mở rộng mượt mà khi hover và thu gọn khi di chuột ra ngoài. | `[ ] Pass`<br>`[ ] Fail` |

---

## 🚀 QUY TRÌNH THỰC HIỆN KIỂM THỬ KHUYÊN DÙNG (TEACHER TEST STEPS)

1. **Bước 1 (Xác thực & Hồ sơ)**: Thực hiện từ **TC-TCH-01** đến **TC-TCH-03D**. Kiểm thử đăng nhập (Positive/Negative/XSS), đăng ký thủ công với OTP 6 số, đăng ký Google, ràng buộc hồ sơ trước khi tạo lớp, đổi mật khẩu, đăng xuất và bảo vệ Route.
2. **Bước 2 (Quản lý Lớp & Socket Real-time)**: Thực hiện từ **TC-TCH-04** đến **TC-TCH-09**. Tạo lớp mới (Positive & Validation & XSS), chuyển đổi Grid/Table, tìm kiếm & lọc, đóng/mở lớp, lưu trữ lớp, nhận thông báo Socket Real-time khi Admin khóa/mở lớp, sao chép mã code, chỉnh sửa lớp.
3. **Bước 3 (Thành viên & Bảng tin)**: Thực hiện từ **TC-TCH-10** đến **TC-TCH-12B**. Phê duyệt đơn xin vào lớp (Single & Approve All & Từ chối), thêm/xóa học sinh (Positive & Negative trùng/không tồn tại), đăng bài thông báo đính kèm file PDF/Word/Video, bình luận công khai.
4. **Bước 4 (Bài tập & Chấm điểm)**: Thực hiện từ **TC-TCH-13** đến **TC-TCH-14B**. Giao bài tập tự luận (Positive & Validation & Boundary), chấm điểm kèm lời phê (Positive & Boundary điểm âm/vượt max), chỉnh sửa điểm, thảo luận với Học sinh.
5. **Bước 5 (Ngân hàng Đề & AI Gemini)**: Thực hiện từ **TC-TCH-15** đến **TC-TCH-24**. Xem/Lọc/Tìm kiếm danh sách Ngân hàng, xem chi tiết modal, soạn đề thủ công, chọn môn học mở rộng, hướng dẫn & file mẫu, import Word/Excel/AI Gemini, chia điểm tự động, chỉnh sửa/xóa học liệu (AlertDialog), giao đề thi cho lớp.
6. **Bước 6 (Điểm danh & Sổ điểm Spreadsheet)**: Thực hiện từ **TC-TCH-25** đến **TC-TCH-26**. Điểm danh hàng ngày (tích chọn/ghi chú/chỉnh sửa), nhập điểm Spreadsheet (click trực tiếp/boundary/đồng bộ/auto ĐTB).
7. **Bước 7 (Thông báo & Sidebar)**: Thực hiện **TC-TCH-27** và **TC-TCH-28**. Kiểm tra Chuông thông báo real-time và menu Sidebar.
