/**
 * ============================================================================
 * TÊN FILE: rootReducer.tsx
 * ĐƯỜNG DẪN: frontend-classroom/src/redux/reducer/rootReducer.tsx
 * MỤC ĐÍCH:
 *   Legacy Root Reducer gom các reducers lại với nhau (File dự phòng/tham khảo cũ).
 * ============================================================================
 */

/* import { combineReducers } from "redux";
import counterReducer from "./counterReducer";
import userReducer from "./userReducer";

const rootReducer = combineReducers({
  counter: counterReducer,
  user: userReducer
});

// Xuất kiểu RootState để dùng cho useSelector
export type RootState = ReturnType<typeof rootReducer>;

export default rootReducer; */