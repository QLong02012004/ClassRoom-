import React from "react";

// Header component - Nay đã được gộp trực tiếp vào Sidebar chính dưới dạng Sub-menu con
// để giải phóng không gian màn hình, loại bỏ thanh sidebar phụ 2 cột.
const Header: React.FC = () => {
  return null;
};

export default Header;
